apiVersion: apps/v1beta1
kind: Deployment
metadata:
  name: {{ template "fullname" . }}
  labels:
    heritage: {{ .Release.Service }}
    release: {{ .Release.Name }}
    chart: {{ .Chart.Name }}-{{ .Chart.Version | replace "+" "_" }}
    component: {{ .Values.Server.ComponentName }}
    app: {{template "name" .}}
spec:
  replicas: {{ .Values.Server.Replicas }}
  template:
    metadata:
      name: {{ template "fullname" . }}
      labels:
        heritage: {{ .Release.Service }}
        release: {{ .Release.Name }}
        chart: {{ .Chart.Name }}-{{ .Chart.Version | replace "+" "_" }}
        component: {{.Values.Server.ComponentName}}
        app: {{template "name" .}}
    spec:
      containers:
      - name: {{ template "fullname" . }}
        image: "{{.Values.Server.Image}}:{{.Values.Server.ImageTag}}"
        imagePullPolicy: "{{.Values.Server.ImagePullPolicy}}"
        env:
        - name: KUBEVIZ_TOKEN
          value: {{ .Values.Server.ApiKey }}
        - name: KUBEVIZ_S3BUCKET
          value: {{ .Values.Server.S3Bucket }}
        - name: KUBEVIZ_S3KEY
          value: {{ .Values.Server.S3Key }}
        - name: "AWS_ACCESS_KEY_ID"
          value: "{{ .Values.Server.AwsAccessKey }}"
        - name: "AWS_SECRET_ACCESS_KEY"
          value: "{{ .Values.Server.AwsSecretKey }}"
